# Voice Settings — Настройки стиля постов
## =========================================
# Этот файл содержит настройки для генерации постов через ИИ.
# Здесь можно менять промпты, тоны и параметры генерации.
# =========================================

# =============================================================================
# НАСТРОЙКИ ГЕНЕРАЦИИ (продублированы в config.py)
# =============================================================================
GENERATION_SETTINGS = {
    # -------------------------------------------------------------------------
    # МОДЕЛЬ ДЛЯ ГЕНЕРАЦИИ
    # -------------------------------------------------------------------------
    # Рекомендуемые модели:
    #   gpt-5.4-mini         — оптимальная цена/качество (230₽ ввод, 1370₽ вывод)
    #   gpt-5.4-nano         — самая дешёвая (61₽ ввод, 380₽ вывод)
    #   gemini-3.1-flash-lite — бюджетная (76₽ ввод, 455₽ вывод)
    #   gpt-5.4             — средняя (760₽ ввод, 4550₽ вывод)
    # -------------------------------------------------------------------------
    'model': 'gpt-5.4-mini',

    # -------------------------------------------------------------------------
    # ТЕМПЕРАТУРА ГЕНЕРАЦИИ (0.0 — строго, 2.0 — творчески)
    # -------------------------------------------------------------------------
    # Низкая (0.3-0.5)   — предсказуемые, стабильные ответы
    # Средняя (0.7-0.9)  — баланс между качеством и оригинальностью
    # Высокая (1.0-1.3)  — творческие, оригинальные ответы
    # -------------------------------------------------------------------------
    'temperature': 0.9,

    # -------------------------------------------------------------------------
    # МАКСИМАЛЬНАЯ ДЛИНА ПОСТА (в токенах)
    # -------------------------------------------------------------------------
    'max_tokens': 800
}

# =============================================================================
# ТОНЫ ПОСТОВ
# =============================================================================
# Каждый тон определяет настроение и стиль генерируемого текста.
# Можно добавлять новые тона или изменять существующие.

TONES = {
    'friendly': {
        'description': 'Дружелюбный',
        'keywords': 'тёплый, дружеский, располагающий, с юмором, улыбка',
        'emotions': 'доброта, радость, теплота, доверие',
        'cta_style': 'мягкий, приглашающий'
    },
    'exciting': {
        'description': 'Восторженный',
        'keywords': 'эмоциональный, энергичный, яркий, вдохновляющий, энтузиазм',
        'emotions': 'восторг, азарт, волнение, преувеличение',
        'cta_style': 'срочный, мотивирующий'
    },
    'professional': {
        'description': 'Деловой',
        'keywords': 'деловой, уверенный, профессиональный, убедительный, факты',
        'emotions': 'уверенность, компетентность, надёжность',
        'cta_style': 'прямой, конкретный'
    },
    'calm': {
        'description': 'Спокойный',
        'keywords': 'спокойный, размеренный, мягкий, умиротворяющий, релакс',
        'emotions': 'спокойствие, гармония, расслабленность',
        'cta_style': 'деликатный, ненавязчивый'
    }
}

# =============================================================================
# ПРОМПТ ДЛЯ СИСТЕМЫ (инструкции для ИИ)
# =============================================================================
# Этот промпт отправляется языковой модели для генерации поста.
# Можно настраивать структуру, требования и формат.

SYSTEM_PROMPT_TEMPLATE = """Ты — эксперт по созданию продающих постов для товаров в социальных сетях.

ЗАДАЧА: Напиши оригинальный, интересный пост для товара.

ТОВАР: {product_name}
ОПИСАНИЕ: {description}
ЦЕНА/ВЫГОДА: {price_info}

ТРЕБОВАНИЯ К ПОСТУ:
1. Заголовок — интригующий, привлекающий внимание (1-2 строки)
2. Основной текст — живой, эмоциональный, с историей или историей успеха
3. Описание пользы товара для покупателя — конкретно и убедительно
4. 4-6 релевантных хэштегов
5. CTA (призыв к действию) — один в конце

ТОН ПОСТА: {tone_description}
КЛЮЧЕВЫЕ СЛОВА ТОНА: {tone_keywords}
ЭМОЦИИ: {tone_emotions}

СТИЛЬ:
- Оригинальность: избегай шаблонных фраз типа "идеальный выбор" или "незаменимая вещь"
- Используй разные эмоции: {tone_emotions}
- Формат: используй эмодзи, но умеренно (2-5 на весь пост)
- Не копируй стиль других продавцов

ФОРМАТ ОТВЕТА:
[заголовок с эмодзи]

[основной текст с эмодзи]

[хэштеги через пробел]

[короткий CTA с эмодзи]"""


# =============================================================================
# ПРАВИЛА ГЕНЕРАЦИИ ХЭШТЕГОВ
# =============================================================================
HASHTAG_RULES = {
    # Максимальное количество хэштегов
    'max_count': 6,
    
    # Обязательные хэштеги (добавляются всегда)
    'required': ['#рекомендую', '#покупки'],
    
    # Запрещённые слова в хэштегах
    'avoid_words': ['дешёвый', 'подделка', 'аналог']
}

# =============================================================================
# ПРАВИЛА ДЛЯ CTA (призыв к действию)
# =============================================================================
CTA_RULES = {
    'direct': [
        'Заказать сейчас',
        'Купить',
        'Оформить заказ'
    ],
    'soft': [
        'Загляните в профиль',
        'Сохраните себе',
        'Добавьте в избранное'
    ],
    'urgency': [
        'Успейте купить!',
        'Осталось немного!',
        'Не упустите момент!'
    ]
}

# =============================================================================
# ЧЕГО ИЗБЕГАТЬ В ПОСТАХ
# =============================================================================
AVOID = {
    # Шаблонные фразы
    'cliches': [
        'идеальный выбор',
        'незаменимая вещь',
        'каждому нужна',
        'супер пупер',
        'вау-вау',
        'ого-го',
        'крутяк',
        'норм',
        'сойдёт',
        'наверное'
    ],
    
    # Запрещённые слова
    'forbidden_words': [
        'никакой', 'ужасный', 'плохой', 'дешёвый', 'подделка',
        'конкурент', 'аналог', 'брак', 'развод', 'лохотрон'
    ]
}
